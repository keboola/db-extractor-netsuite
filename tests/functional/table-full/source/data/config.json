{
  "parameters": {
    "db": {
      "accountId": "${ODBC_ACCOUNT_ID}",
      "roleId":  "${ODBC_ROLE_ID}",
      "user": "${ODBC_USER}",
      "#password": "${ODBC_PASSWORD}"
    },
    "table": {
      "tableName": "Currency",
      "schema": "default"
    },
    "columns": ["InternalId", "CurrencyPrecision", "DisplaySymbol"],
    "outputTable": "in.c-main.currency"
  }
}
